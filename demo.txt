
Steps to add a floder/dir from the coumpter  to git

Open the git bash
cd to the root directory.
mkdir
cd
ls
ls -la

------------------------------

git init testrepo

git add  file.txt

git commit -m "removed notPushableDirectory temporarily"

[git commit -m "removed notPushableDirectory temporarily"  file.txt]

git push -f origin master


-------------------------------